export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'POST only'});if(!process.env.GEMINI_API_KEY)return res.status(503).json({error:'GEMINI_API_KEY is not configured'});try{const body=req.body||{};const market=body.market||{};const timeframe=body.timeframe||'15min';const prompt=`You are GoldX, a disciplined XAUUSD market-analysis agent. Use ONLY the supplied market data. Do not invent candles, news, order flow or indicators. Return concise JSON with keys bias, confidence, entry, stopLoss, tp1, tp2, trend, structure, liquidity, fvgOb, invalidation, reasoning. If candle/SMC evidence is missing, explicitly say so and lower confidence. This is educational analysis, not a guarantee. Timeframe: ${timeframe}. Live spot data: ${JSON.stringify(market)}`;const r=await fetch('https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent',{method:'POST',headers:{'Content-Type':'application/json','x-goog-api-key':process.env.GEMINI_API_KEY},body:JSON.stringify({contents:[{role:'user',parts:[{text:prompt}]}],generationConfig:{responseMimeType:'application/json'}})});const data=await r.json();if(!r.ok)return res.status(502).json({error:'Gemini request failed',details:data});const text=data?.candidates?.[0]?.content?.parts?.[0]?.text||'{}';let result;try{result=JSON.parse(text)}catch{result={reasoning:text}}return res.status(200).json(result)}catch(e){return res.status(500).json({error:e.message})}}