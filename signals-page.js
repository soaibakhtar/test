(function(){function fmt(v){const n=Number(v);return Number.isFinite(n)?n.toFixed(2):'Unavailable'}function render(){const list=JSON.parse(localStorage.getItem('goldxSignals')||'[]');const el=document.getElementById('signalsList');if(!el)return;el.innerHTML=list.length?list.map(s=>`<div class="signal"><b>${s.direction}</b><span>Entry ${s.entry!=null?fmt(s.entry):'Unavailable'}</span><span>SL ${s.sl!=null?fmt(s.sl):'Unavailable'}</span><span>TP1 ${s.tp1!=null?fmt(s.tp1):'Unavailable'}</span><span>${s.time}</span></div>`).join(''):'<div class="empty">No signals yet. Run the AI agent.</div>'}window.clearSignals=function(){localStorage.removeItem('goldxSignals');render()};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',render);else render()})();